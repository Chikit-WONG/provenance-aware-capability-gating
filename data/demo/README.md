# Demo replay fixture

`sample_blocked_attack` is a deterministic, hand-authored UI fixture. It shows
the shape of an exposed attack that the Full gateway blocks while a benign
reply is still completed. It is **not** a formal model run and must never be
included in aggregate experiment results.

The fixture contains only synthetic local identities and side effects.
